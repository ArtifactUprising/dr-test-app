apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-static"
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "{{ join "," .Values.static }}"
spec:
  type: ExternalName
  externalName: "{{ .Values.fastly }}"